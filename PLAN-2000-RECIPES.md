# План: запуск каталога 2000 рецептов БЕЗ картинок (MULTI-CHEF)

> Дата: 2026-09-17. Переработка плана от 2026-09-13: **картинки полностью убраны
> из проекта** (решение владельца). `Recipe.imageKey` остаётся в схеме nullable
> и всегда NULL — дроп колонки не делается. Документ самодостаточен: можно
> скопировать в новую сессию и выполнять по этапам.

## 1. Цель и критерии готовности (Definition of Done)

| #   | Критерий                       | Значение                                                                                                                                |
| --- | ------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Рецептов в статусе `PUBLISHED` | **2000**                                                                                                                                |
| D2  | Описание (1–2 предложения)     | 100%                                                                                                                                    |
| D3  | Инструкции v2                  | ≥4 шагов, суммарно ≥300 символов, у ≥95%                                                                                                |
| D4  | Порция по граммовке            | 100–800 г/порцию у ≥98% (напитки 150–400 мл)                                                                                            |
| D5  | КБЖУ                           | у 100%, `calculationVersion=2`, ккал/порция: напитки 5–500, остальное 120–1200                                                          |
| D6  | Дубликаты названий             | 0 (точных и near-duplicate с trigram similarity ≥0.9)                                                                                   |
| D7  | `IngredientNutrition`          | заполнен для всего справочника (300+ ингредиентов)                                                                                      |
| D8  | Картинки отсутствуют           | `imageKey IS NULL` у 100%; storage-модуль, `@aws-sdk/client-s3`, скрипты генерации фото и UI-рендер изображений удалены из кодовой базы |
| D9  | Приёмочные проверки            | E2E 6/6, генерация плана `/today/generate` p95 < 3 с при 2000, диск ≤ 8G                                                                |
| D10 | Откат                          | каждая волна лежит в git-датасете; откат волны = `status=DRAFT` для её slug-списка                                                      |

**Что НЕ входит в план:** правки алгоритма рекомендаций, новые сущности схемы
(кроме уникального индекса), мобильные клиенты, localization, **генерация и
хранение изображений (окончательно; единственное исключение — nullable-колонка
`imageKey`, которую трогать запрещено)**.

## 2. Текущее состояние (prod `.95` проверен 2026-09-13; датасет — 2026-09-16)

- **269** рецептов `PUBLISHED`/`CURATED`, **300** ингредиентов, справочник
  `IngredientNutrition` **пуст**, `servingGrams=0` у всех.
- Инструкции-скелеты: в среднем 4.6 шага × 42 символа (48% шагов < 40 симв.).
- 53 рецепта (20%) с порцией < 150 г (заниженные `grams` → заниженные ккал:
  «Суп с нутом» 28 ккал).
- Описания отсутствуют у 100%; фото — у 100% и **навсегда** (см. D8).
- **Датасет уже сгенерирован:** `data/recipes/gen/*.json` — **1731 карточка**,
  разбивка по волнам в `data/recipes/waves.json`, ретрофит 269 —
  `data/recipes/retrofit.json`. Поля `imagePrompt`/`imageAlt` в карточках при
  импорте **игнорируются** (перегенерация 1731 файлов ради удаления полей не
  нужна; чистятся попутно на этапе 3).
- **ИЗВЕСТНЫЙ БАГ ДАТАСЕТА:** в инструкциях карточек встречаются «NaN г» и
  «undefined г» — шаблонная ошибка `scripts/generate-recipes.mjs` для
  PIECE/ML-ингредиентов (пример: `frittata-s-brokkoli.json`). Чинится этапом 3.
- API: cursor-пагинация (createdAt+id) — к 2000 готов.
  `RecipeSourceType.IMPORTED` есть в enum — миграция не нужна.
  `Unit`: только `G`, `ML`, `PIECE` — датасет обязан использовать их.
- Сид: upsert по `title` через `findFirst` — **у `title` нет ни unique-индекса,
  ни обычного**; для 2000 переходим на уникальный индекс `lower(title)` + upsert.
- Сид-датасет per-100g КБЖУ ингредиентов существует:
  `packages/database/src/seed/nutrition/nutrition-data.ts`.
- **Фото-инфраструктура, подлежащая демонтажу (этап 0):**
  `apps/api/src/storage/*` (9 файлов, подключён в `app.module.ts`),
  `@aws-sdk/client-s3` в `apps/api`, `scripts/generate-images.mjs`,
  `packages/database/scripts/generate-recipe-images.ts`,
  `tests/e2e/images.spec.ts`, `apps/api/src/__tests__/images.test.ts`,
  UI-рендер в `OptionCard.tsx` / `recipe/[id]/Header.tsx`,
  `apps/web/src/lib/recipe-image.ts`.
- **nginx `location /images/` — НЕ добавлять** (пункт старого плана отменён).
- Диск `.95`: 56G свободно. RAM 4G — импорт только потоково, волнами.

## 3. Этап 0 — демонтаж фото-инфраструктуры (~0.5–1 день)

**Решение по колонке (подтверждено владельцем): `Recipe.imageKey` ОСТАЁТСЯ в
Prisma-схеме как `String?`, миграция дропа НЕ делается.** Инвариант: у всех
строк `NULL`, импорты и API колонку не читают и не пишут.

| #   | Что                                                                                                                                                                                                                                                                                          | Где                     |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| 0.1 | Удалить storage-модуль целиком: `images.controller.ts`, `recipe-images.controller.ts`, `uploads.controller.ts`, `images.service.ts`, `storage.factory.ts`, `local-storage.driver.ts`, `s3-storage.driver.ts`, `storage.ts`, `storage.module.ts`; выпилить `StorageModule` из `app.module.ts` | `apps/api/src/storage/` |
| 0.2 | Убрать зависимость `@aws-sdk/client-s3` и S3-переменные из `apps/api/package.json` + `.env.example` (`check-env-coverage` — ворота)                                                                                                                                                          | apps/api                |
| 0.3 | Вычистить `imageKey` из API-кода: `recipes.service/mappers`, `meal-plans.service`, `recommendations.service`                                                                                                                                                                                 | apps/api                |
| 0.4 | Контракты: убрать `imageKey` из `packages/contracts/src/recipes.ts` + тесты, пересобрать dist                                                                                                                                                                                                | packages/contracts      |
| 0.5 | Web: удалить `lib/recipe-image.ts`; убрать рендер `<img>`/fallback из `OptionCard.tsx` и `recipe/[id]/Header.tsx`; вычистить `recipe-client.ts` и зависимые тесты (rescue/recommendations/roulette)                                                                                          | apps/web                |
| 0.6 | Удалить `scripts/generate-images.mjs`, `packages/database/scripts/generate-recipe-images.ts`, `tests/e2e/images.spec.ts`, `apps/api/src/__tests__/images.test.ts`                                                                                                                            | корень                  |
| 0.7 | `sw.js`/`manifest.webmanifest`: убрать кэш `/images`, если есть                                                                                                                                                                                                                              | apps/web                |

**Ворота этапа:** `grep -rn "imageKey\|imageAlt\|imagePrompt\|StorageModule\|aws-sdk"`
по `apps/`, `packages/*/src`, `scripts/`, `tests/` пуст (кроме
`schema.prisma`, где остаётся только nullable `imageKey`); format + lint +
typecheck + unit зелёные локально; CI зелёный.

## 4. Этап 1 — инфраструктура каталога (~0.5 дня)

```bash
# 1.1 Бэкап перед любыми работами
/opt/multichef/infrastructure/scripts/backup.sh
```

- **1.2 Уникальность названий.** Миграция:
  `CREATE UNIQUE INDEX "Recipe_title_lower_key" ON "Recipe" (lower(title));`
  плюс `CREATE INDEX "Recipe_title_trgm_idx" ON "Recipe" USING gin (title gin_trgm_ops);`
  (для near-dup поиска). Сид-раннер переводится с `findFirst` на upsert по
  `lower(title)`.
- **1.3 `prisma generate` перед любой сборкой** (см. ADR-0022) и сборка worker:
  `pnpm turbo run build --filter=@multichef/worker...`.

## 5. Этап 2 — стандарт качества и справочник (~1 день)

### 5.1 Заполнить `IngredientNutrition` (per 100 г/мл: kcal, proteinG, fatG, carbsG)

- Источник: `packages/database/src/seed/nutrition/nutrition-data.ts`
  (уже есть для 300 ингредиентов).
- Скрипт `scripts/backfill-ingredient-nutrition.ts`: upsert всех 300 →
  выборочная сверка 20 позиций с Open Food Facts / USDA (вручную, таблица в отчёте).
- Это разблокирует: пересчёт КБЖУ любых рецептов и работу пользовательских рецептов.

### 5.2 `servingGrams`

- Формула: `sum(RecipeIngredient.grams) / servings`. Включить в расчёт КБЖУ,
  `calculationVersion → 2`, полная пересчётная миграция всех рецептов.

### 5.3 Валидатор `scripts/validate-recipes.ts` (единые ворота для любого импорта)

Проверяет каждый рецепт (JSON-карточка), выводит отчёт `валид / ошибок по правилам`:

| Правило     | Порог                                                                                                                                         |
| ----------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| Поля        | title (уникальный, ru), category, cuisine, season, mealTypes ≥1, difficulty 1–3, servings 1–12, prep+cook 5–240 мин, description 80–250 симв. |
| Ингредиенты | ≥3; `unit ∈ {G, ML, PIECE}`; `grams > 0`; каждый `ingredientId` резолвится в справочник; сумма граммов > 0                                    |
| Порция      | 100–800 г/порцию; напитки 150–400 мл                                                                                                          |
| КБЖУ        | напитки 5–500 ккал/порцию, остальное 120–1200; protein+fat+carbs × 4/9 в ±25% от kcal                                                         |
| Шаги        | ≥4; суммарно ≥300 символов; ≥1 шаг с числом (время/температура/граммы); шаги не повторяются; **подстроки `NaN` и `undefined` запрещены**      |
| Дедуп       | `lower(title)` уникален + trigram similarity < 0.9 ко всем существующим                                                                       |
| Теги        | `category:` ровно 1, `cuisine:` ≥1, `season:` ≥1                                                                                              |

- Поля `imagePrompt`/`imageAlt` валидатором **не требуются**; при импорте
  молча игнорируются.
- Юнит-тесты валидатора (happy + по одному фейлу на правило, включая NaN-правило).

## 6. Этап 3 — фикс текстового генератора (~0.5 дня)

Баг: `scripts/generate-recipes.mjs` подставляет `grams` в шаблон инструкции и
для штучных/объёмных ингредиентов → «Взбейте 4 яйца (NaN г) с молоком
(undefined г)».

1. Починить шаблонизацию: `PIECE` → «{quantity} × {ingredient}» без граммов,
   `ML` → «{quantity} мл».
2. Скрипт-детектор: найти все карточки `data/recipes/gen/` с `NaN|undefined`
   в инструкциях, перегенерировать/запатчить только затронутые шаги (точечно,
   без пересоздания файлов). Заодно вычистить `imagePrompt`/`imageAlt`.
3. Юнит-тест генератора: NaN/undefined в выходе запрещены (снапшот шаблона).
4. Проверка датасета после фикса:
   `grep -l "NaN\|undefined" data/recipes/gen/*.json` → пусто.

## 7. Этап 4 — контент-пайплайн, только текст (~1.5–2 дня)

### 7.1 Матрица покрытия (целевые 2000, финальные списки категорий сверить с `seed/nutrition/catalog-mapping.ts`)

| Категория                      | Есть    | Цель     | Добрать   |
| ------------------------------ | ------- | -------- | --------- |
| Основные блюда (MAIN)          | ~106    | 550      | ~444      |
| Супы (SOUP)                    | ~41     | 300      | ~259      |
| Завтраки (BREAKFAST)           | ~36     | 280      | ~244      |
| Салаты (SALAD)                 | —       | 220      | 220       |
| Гарниры (SIDE)                 | —       | 180      | 180       |
| Десерты (DESSERT)              | —       | 150      | 150       |
| Напитки (DRINK)                | ~7      | 120      | ~113      |
| Выпечка (BAKING)               | —       | 100      | 100       |
| Заготовки/полуфабрикаты (PREP) | —       | 100      | 100       |
| **Итого**                      | **269** | **2000** | **~1731** |

Уже сгенерированные 1731 карточка (`data/recipes/gen/`) закрывают колонку
«Добрать» — этап сводится к валидации и догенерации выбракованных, а не к
полному циклу генерации.

Кухни (ротация): русская, грузинская, итальянская, французская,
средиземноморская, азиатская, мексиканская, американская, восточная.
Сезон: ALL_YEAR ~60%, остальные 40% на 4 сезона.

### 7.2 Формат датасета

- `data/recipes/wave-N/<slug>.json` — одна карточка = один файл (стриминг, не монолит).
- Схема карточки = текущий `RecipeSeed` + `description`.
  **`imagePrompt`/`imageAlt` не создаются.**
- Хранится в git → ревью, откат, воспроизводимость.

### 7.3 Текстовая генерация `scripts/generate-recipes.ts`

- Вход на карточку: категория + кухня + ключевой ингредиент **из справочника
  300** (новые ингредиенты запрещены; если нужны — карточка уходит в очередь
  `data/ingredient-requests/`, справочник расширяется отдельным шагом с КБЖУ).
- Промпт: JSON-схема, температура 0.7, 2–3 few-shot примера из существующих
  рецептов, запрет повторять существующие названия (передаём список слагов волны).
- Fix-loop: карточка → валидатор → ошибки обратно в LLM, максимум 3 итерации,
  иначе в `data/quarantine/`.
- Инструкции: с учётом фикса этапа 3 — PIECE/ML без граммов в скобках;
  ориентир 4–8 шагов, 300–700 символов, с температурой и временем.
- Граммовки: эвристики порций по категории (супы 300–400 г, основные 250–350,
  салаты 150–250, каши 250–300, напитки 200–300 мл) + `PIECE`-ингредиенты по штукам.
- Оценка объёма: ~1731 карточка × 1–2K токенов ≈ 2–4М токенов (дёшево, гонять
  батчами по 50, с ретраями и чекпойнтами).

Фото-пайплайн старого плана (§5.4: генерация изображений, webp-постобработка,
контакт-листы, бюджет 260 МБ, 3–8 ч генераций) — **удалён**.

### 7.4 Расчёт КБЖУ и импорт

- КБЖУ: `@multichef/nutrition` по `IngredientNutrition` → `RecipeNutrition`
  (`calculationVersion=2`).
- Импорт `scripts/import-recipes.ts`: upsert по `lower(title)`, `status=DRAFT`,
  полная замена `RecipeIngredient` (как текущий сид), запись nutrition.
  **`imageKey` не пишется.**
- Публикация волны: после QA —
  `UPDATE "Recipe" SET status='PUBLISHED' WHERE id = ANY($wave_ids)`.

## 8. Этап 5 — волны импорта (0.5 дня на волну)

| Волна | Объём | Итог каталога | Ворота выхода                                                                |
| ----- | ----- | ------------- | ---------------------------------------------------------------------------- |
| 1     | +331  | ~600          | валидатор 0 ошибок; QA 30 карточек глазами; E2E 6/6; `/today/generate` < 3 с |
| 2     | +700  | ~1300         | то же + нагрузочная проверка поиска                                          |
| 3     | +700  | **2000**      | полное DoD из §1                                                             |

Перед каждой волной: `backup.sh`. Откат волны: `status=DRAFT` по списку slug
(данные не удалять — каталог в git).

## 9. Этап 6 — ретрофит существующих 269 (~0.5 дня, тем же пайплайном)

1. 53 «тонких» рецепта (порция < 150 г): пересборка граммовок по эвристикам §7.3.
2. Инструкции v2 (раскрытие скелетов), описания.
3. Пересчёт КБЖУ v2 всех 269 (включая 7 выбросов ккал: проверить чай/кофе
   отдельно — там норма).

Фото — пункт удалён (их 0% и не будет).

## 10. Этап 7 — приёмка (готовые контрольные запросы)

```sql
-- D1: count
SELECT count(*) FROM "Recipe" WHERE status='PUBLISHED';                          -- 2000
-- D8: картинки отсутствуют (инвариант nullable-колонки)
SELECT count(*) FILTER (WHERE "imageKey" IS NOT NULL) FROM "Recipe";             -- 0
-- D4/D5: порции и ккал
SELECT count(*) FILTER (WHERE portion NOT BETWEEN 100 AND 800) FROM
  (SELECT r.id, sum(ri.grams)/r.servings portion FROM "Recipe" r
   JOIN "RecipeIngredient" ri ON ri."recipeId"=r.id GROUP BY r.id, r.servings) p; -- <40 (2%)
-- D5: version
SELECT count(*) FROM "RecipeNutrition" WHERE "calculationVersion" < 2;           -- 0
-- D7: справочник
SELECT count(*) FROM "Ingredient" i WHERE NOT EXISTS
  (SELECT 1 FROM "IngredientNutrition" n WHERE n."ingredientId"=i.id);           -- 0
-- D6: near-dup
SELECT count(*) FROM (SELECT t FROM
  (SELECT lower(title) t FROM "Recipe") a JOIN (SELECT lower(title) t FROM "Recipe") b
  ON a.t < b.t AND similarity(a.t, b.t) >= 0.9) x;                               -- 0
```

Плюс: E2E `E2E_BASE_URL=http://192.168.1.95:8080 pnpm --filter @multichef/web
test:e2e` → 6/6 (без `images.spec.ts` — удалён на этапе 0); замер p95
`/today/generate`; `df -h /` ≤ 8G занято. Проверка кодовой базы: сборка API/web
проходит без S3 — `grep aws-sdk pnpm-lock.yaml` → пусто.

## 11. Риски и меры

| Риск                                             | Мера                                                                                |
| ------------------------------------------------ | ----------------------------------------------------------------------------------- |
| LLM придумывает ингредиенты вне справочника      | жёсткий резолв по `ingredientId`, иначе quarantine + очередь расширения справочника |
| Семантические дубли («Борщ» / «Борщ украинский») | trigram-порог + ручная QA-выборка 5% волны                                          |
| NaN/undefined в инструкциях датасета             | этап 3: фикс шаблона + правило валидатора + grep-ворота перед импортом              |
| 4GB RAM при импорте                              | потоковая обработка по одному файлу, волны ≤800                                     |
| Долгий сид при 2000 (findFirst без индекса)      | индекс `lower(title)` на этапе 1 + upsert                                           |
| Стоимость/лимиты генерации                       | чекпойнты, ретраи, батчи 50; остаётся только текстовая генерация                    |
| Демонтаж фото ломает скрытые зависимости         | ворота этапа 0: grep + lint + typecheck + unit + CI до каких-либо импортов          |

## 12. Порядок исполнения (сводка)

1. Этап 0 демонтаж (0.5–1 д) → 2. Этап 1 (0.5 д) → 3. Этап 2 (1 д) →
2. Этап 3 фикс генератора (0.5 д) → 5. Этап 4 контент (1.5–2 д) →
3. Волны 1–3 (2 д) → 7. Ретрофит 269 (0.5 д) → 8. Приёмка (0.5 д).
   **Итого ориентировочно: 4.5–6 рабочих дней**, из них ожидание генераций — фоном.
   (Старая оценка 7–9 дней уменьшилась за счёт удаления фото-пайплайна.)

---

## Промт для новой сессии (копировать отсюда)

```
Ты — devops/бэкенд-инженер MULTI-CHEF. Выполни план PLAN-2000-RECIPES.md
(лежит в корне репо /opt/multichef на 192.168.1.95, ssh root, вход по ключу).
Прод: http://192.168.1.95:8080, ветка main. ВАЖНО: проект БЕЗ КАРТИНОК —
Recipe.imageKey в схеме остаётся nullable и всегда NULL (не дропать, не
заполнять); фото не генерируются, nginx /images/ не настраивается. Работай
по этапам 0→7 плана, после каждого этапа — коммит и пуш в main (pre-commit:
gitleaks установлен, commitlint: заголовок ≤72 симв.). Правила deployment:
только deploy.sh на .95 (см. ADR-0022: prisma generate перед сборкой, worker
собирается отдельно). Не публиковай волну без зелёного валидатора и
QA-выборки. Отчёт по каждому этапу: что сделано, цифры, артефакты.
```
